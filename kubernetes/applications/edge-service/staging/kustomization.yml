apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - github.com/kkamdung/edge-service/k8s?ref=main

configMapGenerator:
  - behavior: merge
    files:
      - patch-env.yml
    name: edge-config
